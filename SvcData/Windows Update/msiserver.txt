# Windows Installer
# Adds, modifies, and removes applications provided as a Windows Installer (*.msi, *.msp) package.
Manages *.msi installations. Low-level installation of assemblies is done by Trusted Installer, this service oversees the MSI package level.
