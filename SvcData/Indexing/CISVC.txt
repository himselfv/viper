# Indexing Service
Indexes the contents and properties of files on local and remote computers and provides quick search capability through a flexible querying language.

Extracts all textual information. Updates index whenever a file is created, modified, or deleted.

Replaced by Windows Search in Windows 7 and Windows Server 2008 R2. Still available but must be explicitly installed.
